{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "BaseLine",
  "main": "node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development nodemon -x \"clear;node\" -r dotenv/config src/app.js ",
    "start": "NODE_ENV=production node -r dotenv/config src/app.js "
  },
  "author": "Edin",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "knex": "^0.21.20",
    "nodemon": "^2.0.12",
    "pg": "^8.6.0",
    "pg-pool": "^3.3.0",
    "sequelize": "^6.6.5"
  },
  "devDependencies": {
    "sequelize-cli": "^6.2.0"
  }
}
